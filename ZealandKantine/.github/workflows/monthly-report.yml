name: Monthly Report

on:
  schedule:
    - cron: '0 8 20 * *'
  workflow_dispatch:

jobs:
  trigger-report:
    runs-on: ubuntu-latest
    steps:
      - name: Call monthly report endpoint
        run: |
          curl -X POST https://cafezea.dk/MonthlyStatements/GenerateMonthlyStatement